# Nested Container Support

This folder contains the manifests to create the container image that is used for this workspace - `quay.io/cgruver0/che/ocp-4-20-nested-containers:latest`

The Containerfile and entrypoint have comments that explain the internals.
